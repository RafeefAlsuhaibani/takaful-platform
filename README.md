# Takaful Platform (Monorepo)

## Structure
- `backend/` — Python/Django app, tested with `pytest`.
- `frontend/` — React app (Node 20), built with `npm run build`.
- `.github/workflows/` — CI workflows.
- `.github/ISSUE_TEMPLATE/` — issue templates & config.
